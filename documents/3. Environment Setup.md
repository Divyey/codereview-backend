 Environment Setup

    Creating and activating a virtual environment

    Installing dependencies (pip install -r requirements.txt)

    Setting up the .env file (list and explain all required environment variables, e.g., DATABASE_URL, SECRET_KEY, GITHUB_WEBHOOK_SECRET, etc.)

    Example .env file

FERNET_KEY - GENERATION IN TERMINAL
```
python
```
>>> from cryptography.fernet import Fernet
... print(Fernet.generate_key().decode())
